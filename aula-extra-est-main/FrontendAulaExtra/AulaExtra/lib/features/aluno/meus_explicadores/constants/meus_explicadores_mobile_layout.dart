import 'package:flutter/material.dart';

class MeusExplicadoresMobileLayout {
  const MeusExplicadoresMobileLayout._();

  static const pageBackground = Color(0xFFF9FAFB);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF101828);
  static const textSecondary = Color(0xFF4A5565);
  static const textMuted = Color(0xFF6A7282);
  static const accentOrange = Color(0xFFFC9039);
  static const accentPink = Color(0xFFF15C64);

  static const horizontalPadding = 16.0;
  static const topPadding = 20.0;
  static const sectionSpacing = 24.0;
  static const cardRadius = 24.0;
  static const cardWidth = 304.0;
  static const cardSpacing = 14.0;
}
